[project]
name = "dzesen-news"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "django",
  "django-vtasks",
  "psycopg",
  "perplexityai",
]

[project.optional-dependencies]
dev = [
  "ruff",
]

[tool.uv]
package = false
